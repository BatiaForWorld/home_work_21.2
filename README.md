# 21.2 Основы верстки


###  Чтение HTML-файла через open()
Содержимое HTML-файла считывается с помощью функции `open()` с кодировкой UTF-8:


###  Замена Content-Type с application/json на text/html
Вместо JSON сервер отправляет:
```
self.send_header("Content-type", "text/html; charset=utf-8")
```

###  Bootstrap верстка
Собраны HTML-документы со структурой страниц и подключенными Bootstrap-стилями:
- Главная страница (index.html)
-  Страница контактов (templates/contacts.html)
- Каталог товаров (templates/catalog.html)
- Категория товаров (templates/category.html)

Все файлы подключают Bootstrap локально

###  Запустите приложение
```
python3 main.py
```

###  Откройте браузер
Введите в адресной строке:
```
http://127.0.0.1:8080/
```

Вы видите страницу "Контакты" с формой!

## Проверка работы приложения

### Через браузер
1. Откройте `http://127.0.0.1:8080/`
2. Вы увидите страницу "Контакты"
3. Заполните форму и отправьте (GET запрос)

### Через curl (проверка Content-Type)
```
curl -I http://127.0.0.1:8080/
```



### Через curl (получить HTML)
```
curl http://127.0.0.1:8080/ | head -30
```

##  Остановка приложения

В терминале нажмите:
```
Ctrl + C
```

Вы увидите:
```
🛑 Server stopped.
```
